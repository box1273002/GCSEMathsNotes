import { Inter, Shadows_Into_Light } from "next/font/google";
export const inter = Inter({subsets: ['latin']});
export const shadowsIntoLight = Shadows_Into_Light({subsets: ['latin'], weight:['400']});
